package com.example.myapo;

import android.app.Activity;

/** Legacy toolbar helper — kept for source compatibility only. Not used by upgraded activities. */
public class mViewToolbar {
    public static void MFnmodsToolbar(Activity ct) {
        // No-op in the upgraded version — replaced by MaterialToolbar in XML.
    }
}
