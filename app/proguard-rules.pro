# Add project specific ProGuard rules here.
# Currently minifyEnabled is false, so this file has no effect yet.
# If you enable minification later, add keep rules for reflection-based
# resource lookups (getIdentifier) here, since ProGuard/R8 can't trace
# string-based resource names automatically.
